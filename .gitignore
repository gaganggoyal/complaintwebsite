# dependencies
node_modules/

# secrets & local environment (keep .env.example tracked)
.env
.env.local
.env.bak.*

# private operational notes — server addresses, paths, credentials location.
# This repository is public; these must never be committed.
*.local.md

# database & runtime data
server/data/
*.db
*.db-shm
*.db-wal
*.sqlite

# logs
*.log

# OS / editor cruft
.DS_Store
Thumbs.db
